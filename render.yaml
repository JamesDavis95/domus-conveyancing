services:
  - type: web
    name: domus-global
    env: python
    buildCommand: pip install -r requirements-minimal.txt
    startCommand: python ultra_minimal.py
    envVars:
      - key: PORT
        sync: false