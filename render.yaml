services:
  - type: web
    name: domus-global
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run_server.py
    envVars:
      - key: PORT
        sync: false
    disk:
      name: domus-data
      mountPath: /app/data
      sizeGB: 1