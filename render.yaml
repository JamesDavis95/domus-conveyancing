services:
  - type: web
    name: domus-planning-platform
    env: python
    plan: starter
    buildCommand: pip install fastapi uvicorn
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health