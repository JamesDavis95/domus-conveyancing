services:
  - type: web
    name: domus-global
    env: python
    buildCommand: pip install -r requirements-minimal.txt
    startCommand: python app.py
    envVars:
      - key: PORT
        sync: false